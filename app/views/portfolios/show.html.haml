%div{ :id => "footer" }
	%div{ :class => "container" }
		%div{ :id => "text-container" }
			%div{ :class => "col-md-offset-2 col-md-8" }
				%br
				%h2{ :class => "orange" }
					= @portfolio.title

				%h5{ :class => "italic" }
					= @portfolio.date

				%h5{ :class => "grey" }
					=markdown(@portfolio.content).html_safe
				%h5
					%a{ :href => "/work/" } Back
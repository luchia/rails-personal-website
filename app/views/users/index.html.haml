#footer
  %section
    .container
      #text-container
        .row
          .col-md-8.col-md-offset-2
            %h2.orange Listing Users
            %br

            -@users.each do |user|
              %p=user.name
              %br
              %p.text-right=link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }

            %br
            %p=link_to 'New User', new_user_path

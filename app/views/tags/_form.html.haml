= form_for(@tag) do |f|
  = if @tag.errors.any?
    #error_explanation
      %h2
        = pluralize(@tag.errors.count, "error") 
        prohibited this tag from being saved:

      %ul
      = @tag.errors.full_messages.each do |msg|
        %li 
          = msg
      %br

  .field
    = f.text_field :tag, placeholder: "Tag title"

  .actions
    = f.submit
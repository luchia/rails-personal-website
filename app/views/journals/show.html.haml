#footer
	.container
		#text-container
			.col-md-offset-2.col-md-8
				%br
				%h2.orange
					= @journal.title

				%p
					= @journal.created_at.strftime("%d %b, %Y")

				%p
					=markdown(@journal.content).html_safe

				%p
					=link_to 'Back', '/blog'